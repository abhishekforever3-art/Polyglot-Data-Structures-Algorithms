/* public class DataPractice {
    public static void main(String[] args) {
        // --- NUMBERS ---
        int followers = 1500;
        double price = 49.99;
        long worldPopulation = 8000000000L; // Note the 'L'
        
        // --- LOGIC & CHARS ---
        boolean isJavaFun = true;
        char currency = '$'; // Single quotes only
        
        // --- TEXT (Non-Primitive) ---
        String message = "Your total is: ";

        // Printing and combining (Concatenation)
        System.out.println(message + currency + price);
        System.out.println("Followers: " + followers);
        System.out.println("Is Java Fun? " + isJavaFun);
    }
}
 */